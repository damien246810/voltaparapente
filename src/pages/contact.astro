---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout>
    <Navbar />
  <!-- Section héro -->
  <section 
    class="relative h-screen bg-cover bg-center flex items-center justify-center text-white"
    style="background-image: url('/images/vol2.jpg');"
  >
    <!-- Overlay sombre -->
    <div class="absolute inset-0 bg-black/10"></div>

    <!-- Contenu centré -->
    <div class="relative z-10 text-center space-y-8">
    <h1 class="text-5xl font-bold">Des questions ?</h1>

    <!-- Bouton vers formulaire -->
    <a href="#contact" class="inline-flex items-center gap-2 bg-yellow-400 hover:bg-yellow-500 text-black font-semibold px-6 py-3 rounded-full transition">
        Contactez-nous
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 animate-bounce" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
        </svg>
    </a>
    </div>
  </section>

  <!-- Formulaire de contact -->
    <section id="contact" class="container mx-auto px-5 py-16 scroll-mt-6">
    
    <h1 class="text-center text-4xl font-bold mb-4">Contactez-nous</h1>

    <!-- Trois cases horizontales -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-center mb-8">
        <!-- WhatsApp -->
        <div class="bg-gray-200 p-6 rounded-xl flex flex-col items-center justify-center space-y-3">
            <!-- Bouton WhatsApp -->
            <a 
                aria-label="Chat on WhatsApp" 
                href="https://wa.me/41792032073"
                target="_blank"
                class="inline-block mx-auto"
            >
                <img 
                src="/images/whatsapp.png" 
                alt="Chat on WhatsApp" 
                class="w-full h-auto max-w-[300px]"
                />
            </a>
        </div>

        <!-- Téléphone -->
        <div class="bg-gray-200 p-6 rounded-xl flex flex-col items-center justify-center space-y-2">
        <div class="text-5xl">📞</div>
        <p class="text-2xl font-semibold">Appelez-nous</p>
        <a href="tel:+41792032073" class="text-yellow-400 font-medium hover:text-yellow-500 transition">
            +41 79 203 20 73
        </a>
        </div>

        <!-- Email -->
        <div class="bg-gray-200 p-6 rounded-xl flex flex-col items-center justify-center space-y-2">
        <div class="text-6xl">📧</div> <!-- Emoji email -->
        <p class="text-2xl font-semibold">Écrivez-nous</p>
        <a href="mailto:info@volta-parapente.com" class="text-yellow-400 font-medium hover:text-yellow-500 transition">
            info@volta-parapente.com
        </a>
        </div>
    </div>

    <h1 class="text-center text-2xl font-bold">Formulaire de contact</h1>

    <!-- Formulaire centré -->
    <div class="mt-4 flex justify-center">
        <form class="w-full md:w-3/4 mx-auto bg-gray-200 p-6 rounded-xl shadow-md space-y-4"
              action="https://formspree.io/f/mkgvaroz"
              method="POST">

        <!-- Ligne 1 : Nom et Prénom -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
            <label for="firstName" class="block mb-1 font-medium">Prénom</label>
            <input type="text" id="firstName" name="firstName" required
                    class="w-full p-3 rounded-lg border border-black/50 focus:ring-2 focus:ring-yellow-400 text-black" />
            </div>
            <div>
            <label for="lastName" class="block mb-1 font-medium">Nom</label>
            <input type="text" id="lastName" name="lastName" required
                    class="w-full p-3 rounded-lg border border-black/50 focus:ring-2 focus:ring-yellow-400 text-black" />
            </div>
        </div>

        <!-- Ligne 2 : Email -->
        <div>
            <label for="email" class="block mb-1 font-medium">Email</label>
            <input type="email" id="email" name="email" required
                class="w-full p-3 rounded-lg border border-black/50 focus:ring-2 focus:ring-yellow-400 text-black" />
        </div>

        <!-- Ligne 3 : Téléphone -->
        <div>
            <label for="phone" class="block mb-1 font-medium">Téléphone</label>
            <input type="tel" id="phone" name="phone"
                class="w-full p-3 rounded-lg border border-black/50 focus:ring-2 focus:ring-yellow-400 text-black" />
        </div>

        <!-- Ligne 4 : Message -->
        <div>
            <label for="message" class="block mb-1 font-medium">Message</label>
            <textarea id="message" name="message" rows="5"
                    class="w-full p-3 rounded-lg border border-black/50 focus:ring-2 focus:ring-yellow-400 text-black"></textarea>
        </div>

        <!-- Bouton Envoyer -->
        <div class="flex justify-center">
        <button type="submit"
                class="w-full md:w-1/2 py-3 bg-yellow-400 text-black font-semibold rounded-lg hover:bg-yellow-500 transition">
            Envoyer
        </button>
        </div>
        </form>

    </div>
    </section>


  <Footer />
</Layout>
